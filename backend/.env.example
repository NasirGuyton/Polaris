# Database
DB_HOST=localhost
DB_PORT=5432
DB_NAME=polaris
DB_USER=your_user
DB_PASSWORD=your_password
DB_POOL_MAX=10

# Server
PORT=3001
CORS_ORIGIN=http://localhost:3000

# Survey
DEFAULT_SURVEY_ID=1

# Admin — required to access export, stats, and response endpoints
ADMIN_API_KEY=change-me-to-a-random-secret
